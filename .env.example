# =============================================================================
# StudySnap AI — Environment Variables
# Copy to .env.local for local development. Never commit secrets.
# =============================================================================

# --- Public (exposed to browser via NEXT_PUBLIC_*) ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=https://dmwixrrtzxtbwndzalqq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-from-dashboard-api-settings

# --- Server-only (never prefix with NEXT_PUBLIC_) ---
# Optional — bypasses RLS; NOT required for OCR/uploads in this app. Never use NEXT_PUBLIC_ prefix.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenAI — required for AI summaries (server-only)
OPENAI_API_KEY=sk-...

# Optional — analytics, error tracking (Week 2+)
# NEXT_PUBLIC_POSTHOG_KEY=
# SENTRY_AUTH_TOKEN=
